{
  "buildCommand": "cd TraceX-main/frontend && npm install && npm run build",
  "outputDirectory": "TraceX-main/frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
